<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lk.fmt.findmytutor">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".application.FMTApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.CourseDetailsActivity"></activity>
        <activity android:name=".activities.CoursesActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.TutorProfileActivity"></activity>
        <activity
            android:name=".activities.LoadingActivity"
            android:theme="@style/LoadingTheme" />
        <activity
            android:name=".activities.SignInActivity"
            android:theme="@style/SignInTheme"></activity>
        <activity
            android:name=".activities.Splash"
            android:theme="@style/SplashTheme">
            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity android:name=".activities.HomeActivity"></activity>
    </application>

</manifest>